#include <bits/stdc++.h>
#include <vector>
using namespace std;
 
int main() {
    int t;
    cin >> t;
    
    while (t--) {
        // cout << "case " << t << ":\n";
        int n, m;
        cin >> n >> m;

        if (n == 1 && m == 1) {
            cout << 0 << end;
            continue;
        }

        if (m == 1) {
            cout << 1 << endl;
            continue;
        }

        if (n == 1) {
            cout << -1 << endl;
            continue;
        

        

        

        cout << ans << endl;
    }
}